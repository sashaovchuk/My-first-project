import { FilterProdBrNamePipe } from './filter-prod-br-name.pipe';

describe('FilterProdBrNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProdBrNamePipe();
    expect(pipe).toBeTruthy();
  });
});
