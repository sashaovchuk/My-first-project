import { PordFirstPipe } from './pord-first.pipe';

describe('PordFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new PordFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
