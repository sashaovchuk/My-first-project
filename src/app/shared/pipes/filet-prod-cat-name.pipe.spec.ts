import { FiletProdCatNamePipe } from './filet-prod-cat-name.pipe';

describe('FiletProdCatNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FiletProdCatNamePipe();
    expect(pipe).toBeTruthy();
  });
});
